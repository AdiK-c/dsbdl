// Import Spark
import org.apache.spark.sql.SparkSession

// Create Spark session
val spark = SparkSession.builder()
    .appName("WordCount")
    .master("local[*]")
    .getOrCreate()

// Read text file
val data = spark.read.textFile("input.txt")

// Perform word count
val words = data.flatMap(line => line.split(" "))
val wordCount = words.groupBy(word => word).count()

// Show result
wordCount.show()




using sacla shell

🔹 1. Install Java (required)
sudo apt update
sudo apt install openjdk-11-jdk -y

Check:

java -version
🔹 2. Download Spark
wget https://downloads.apache.org/spark/spark-3.5.1/spark-3.5.1-bin-hadoop3.tgz
🔹 3. Extract Spark
tar -xvzf spark-3.5.1-bin-hadoop3.tgz
mv spark-3.5.1-bin-hadoop3 spark
🔹 4. Set Environment Variables
nano ~/.bashrc

👉 Add this at bottom:

export SPARK_HOME=~/spark
export PATH=$PATH:$SPARK_HOME/bin:$SPARK_HOME/sbin

Apply:

source ~/.bashrc
🚀 ✅ COMPLETE STEPS (spark-shell WordCount)
# 1. Start Spark Shell
spark-shell
# 2. Create input file (in another terminal)
nano input.txt

👉 Add this text:

hello world hello spark

👉 Save: CTRL + X → Y → ENTER

Now go back to spark-shell and run ALL steps:
// 3. Load file
val data = sc.textFile("input.txt")

// 4. Split words
val words = data.flatMap(line => line.split(" "))

// 5. Map words
val pairs = words.map(word => (word, 1))

// 6. Reduce to count
val counts = pairs.reduceByKey((a, b) => a + b)

// 7. Display result
counts.collect()
